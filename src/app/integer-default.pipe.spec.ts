import { IntegerDefaultPipe } from './integer-default.pipe';

describe('IntegerDefaultPipe', () => {
  it('create an instance', () => {
    const pipe = new IntegerDefaultPipe();
    expect(pipe).toBeTruthy();
  });
});
